# 项目规则手册（给Copilot Agent）
## 仓库用途
本仓库是C语言代码练习仓库，用于Coding Agent实验。
仓库内只有简单C代码文件，用来做bug修复、代码演示。

## 文件组织
- 所有C源码：*.c
- 编译产物（bug.exe等可执行文件）不需要提交到git，属于临时文件
- 仓库只保留源码、README，不要自动新增无关文件

## 开发&修改规则
1. 修改代码之后，**必须编译运行，检查运行结果是否正确**，确认修复有效。
2. 不确定代码逻辑、不确定需求的时候，**禁止自己猜测，直接提问**。
3. 严禁写入任何密码、Token、API密钥等敏感信息到仓库任何文件。
4. 修改文件前，先告知我你准备修改哪个文件、改动哪一行。
5. 不要自动执行git commit、git push，**提交代码前必须先征得我的同意**。
6. 只修改任务指定的文件，不改动仓库里其他无关文件。